import { createNewLiguiInstance } from '../src/ligui';

describe('Ligui test', () => {

    it('createNewLiguiInstance', () => {
        createNewLiguiInstance({
            name: 'TestLigui'
        })
    });

});
