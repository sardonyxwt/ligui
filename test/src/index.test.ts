import { createNewLiguiInstance } from '@source';

describe('Ligui test', () => {
    it('createNewLiguiInstance', () => {
        createNewLiguiInstance({
            name: 'TestLigui',
        });
    });
});
